<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marketapp">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketApp">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScanProductCodeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ProductDetailActivity"
            android:parentActivityName=".ScanProductCodeActivity" />
        <activity
            android:name=".ShoppingCartActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".VoucherActivity"
            android:parentActivityName=".ScanProductCodeActivity" />
        <activity
            android:name=".PastTransactionsActivity"
            android:parentActivityName=".PastTransactionsActivity" />
    </application>

</manifest>